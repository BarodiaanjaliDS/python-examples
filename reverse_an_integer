# program to reverse an interger

num = int(input("Enter an integer: "))
reverse = 0
while ( num != 0):
    reverse = reverse*10 + num%10
    num = num//10
print("Resever interger: {}".format(reverse))
